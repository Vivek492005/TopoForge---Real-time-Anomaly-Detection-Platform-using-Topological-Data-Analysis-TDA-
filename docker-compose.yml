version: '3.8'

services:
  frontend:
    build: .
    ports:
      - "3000:80"
    restart: always

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    restart: always
